spring:
  datasource:
    url: jdbc:h2:mem:testdb
  shell:
    interactive:
      enabled: false
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    change-log: db/changelog/db.changelog.yml
    enabled: true
